<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.nsu.yousightseeing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="ru.nsu.yousightseeing.features.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 🔹 Заставка (UI) -->
        <activity
            android:name="ru.nsu.yousightseeing.features.SplashScreenActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 Логика навигации -->
        <activity
            android:name="ru.nsu.yousightseeing.features.SplashActivity"
            android:exported="false" />

        <activity
            android:name="ru.nsu.yousightseeing.features.AuthActivity"
            android:exported="false" />

        <activity
            android:name="ru.nsu.yousightseeing.features.PreferencesActivity"
            android:exported="false" />

        <activity
            android:name="ru.nsu.yousightseeing.features.PermissionActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <activity
            android:name="ru.nsu.yousightseeing.features.MainActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <activity
            android:name="ru.nsu.yousightseeing.features.ProfileActivity"
            android:exported="false" />

    </application>

</manifest>
